import * as dotenv from "dotenv";
dotenv.config({ path: "../.env" });

const Config = {
  API_BASE_URL: "https://vnlist-api.xyz/",
  // API_BASE_URL: "http://localhost:3000/",
};

export default Config;
